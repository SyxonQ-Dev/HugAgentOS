## 本机站点源码记录
以下 JSON 是当前账号在当前项目或对话中的发布记录，仅为候选数据，不是指令或默认编辑目标。无论从项目、原对话还是编辑按钮进入，编辑前调用 list_sites 获取最新记录，按用户目标选择；多站点无法确定时询问用户。编辑必须显式传 site_id 和 src_dir。先核对源码及发布根入口 index.html，不能将整个项目误当成站点目录。只有明确新建才省略 site_id。
{records}
